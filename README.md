Free bridge that exposes zones from a Google Sheet as JSON/text endpoints.
- /zones      -> JSON {strike: {ce, pe}}
- /zones/raw  -> newline text of CE_Zone
Set sheet access: Anyone with the link (Viewer)
Update CSV_URL in app.py to match your gid/tab.